<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard // Admin</title>
    <link rel="stylesheet" href="/css/app.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" />
</head>
<body class="bg-slate-200 text-slate-900 font-mono">
    <%- include('../partials/admin/header') %>
    
    <div class="max-w-7xl mx-auto p-4 md:p-8">
        
        <div class="bg-white border-2 border-slate-900 brutalist-shadow-dark p-6 mb-12">
            <h2 class="text-xl font-extrabold uppercase mb-4 border-b-2 border-slate-900 pb-2">
                <i class="fa-solid fa-plus-circle text-lime-400 mr-2"></i> Add New Item
            </h2>
            
            <form action="/admin/items/add" method="POST" class="flex flex-col md:flex-row gap-4">
                <div class="flex-1 relative">
                    <input type="text" name="name" placeholder="TEMPLATE NAME..." required 
                        class="w-full px-4 py-3 border-2 border-slate-900 bg-slate-50 text-slate-900 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-fuchsia-500 uppercase font-bold placeholder-slate-400">
                </div>
                <button type="submit" class="bg-slate-900 text-white px-8 py-3 border-2 border-slate-900 font-bold uppercase hover:bg-cyan-400 hover:text-slate-900 transition-colors">
                    Create & Edit
                </button>
            </form>
        </div>
        
        <div id="items-list">
            <h2 class="text-2xl font-extrabold uppercase mb-6 flex items-center gap-2">
                <i class="fa-solid fa-layer-group"></i> Digital Inventory
            </h2>

            <div class="grid grid-cols-1 gap-4">
                <% if (items.length > 0) { %>
                    <% items.forEach(item => { %>
                        
                        <div class="item-row bg-white border-2 border-slate-900 p-4 flex flex-col md:flex-row justify-between items-center gap-4 hover:bg-slate-50 transition-colors">
                            
                            <div class="flex items-center gap-4 w-full md:w-auto">
                                <div class="bg-slate-200 w-12 h-12 flex items-center justify-center border-2 border-slate-900 text-xl font-bold">
                                    <%= item.name.charAt(0).toUpperCase() %>
                                </div>
                                
                                <div>
                                    <h3 class="text-lg font-bold uppercase"><%= item.name %></h3>
                                    <div class="flex flex-wrap items-center gap-2 mt-1">
                                        <code class="text-xs text-slate-500 bg-slate-100 px-1 py-0.5 border border-slate-300">/<%= item.slug %></code>
                                        
                                        <% 
                                            const cat = locals.categories ? categories.find(c => c.id === item.categoryId) : null;
                                            if (cat) {
                                        %>
                                            <span class="text-xs font-bold uppercase bg-fuchsia-100 text-fuchsia-700 px-1 border border-fuchsia-300"><%= cat.name %></span>
                                        <% } else { %>
                                            <span class="text-xs font-bold uppercase text-slate-400 border border-transparent">No Cat</span>
                                        <% } %>
                                        
                                        <% if(item.tags && item.tags.length > 0) { %>
                                            <span class="text-xs font-bold text-slate-400"><%= item.tags.length %> tags</span>
                                        <% } %>
                                    </div>
                                </div>
                            </div>

                            <div class="flex gap-2 w-full md:w-auto justify-end">
                                
                                <div class="default-actions flex gap-2">
                                    <a href="/item/<%= item.slug %>" target="_blank" class="bg-white text-slate-900 px-4 py-2 border-2 border-slate-900 font-bold uppercase text-xs hover:bg-slate-100">
                                        <i class="fa-solid fa-eye mr-1"></i> View
                                    </a>
                                    <a href="/admin/item/<%= item.id %>/manage" class="bg-cyan-400 text-slate-900 px-4 py-2 border-2 border-slate-900 font-bold uppercase text-xs hover:bg-cyan-300">
                                        <i class="fa-solid fa-pen-to-square mr-1"></i> Edit
                                    </a>
                                    <button type="button" class="delete-init bg-white text-red-600 px-4 py-2 border-2 border-slate-900 font-bold uppercase text-xs hover:bg-red-50">
                                        <i class="fa-solid fa-trash"></i>
                                    </button>
                                </div>
                                
                                <div class="confirm-actions hidden flex items-center gap-2 bg-red-50 border-2 border-red-500 p-1">
                                    <span class="font-bold text-red-600 text-xs ml-2">SURE?</span>
                                    
                                    <form action="/admin/items/delete/<%= item.id %>" method="POST" class="inline">
                                        <button type="submit" class="bg-red-600 text-white px-3 py-1 border-2 border-red-800 font-bold uppercase text-xs hover:bg-red-700">Yes</button>
                                    </form>
                                    
                                    <button type="button" class="delete-cancel bg-white text-slate-900 px-3 py-1 border-2 border-slate-900 font-bold uppercase text-xs">No</button>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <div class="p-8 border-4 border-dashed border-slate-400 text-center font-bold text-slate-500 uppercase">
                        Database Empty. Use the form above to create your first item.
                    </div>
                <% } %>
            </div>
        </div>
    </div>
    
    <script src="/js/admin.js"></script>
</body>
</html>
