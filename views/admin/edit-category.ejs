<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit Category // Admin</title>
    <link rel="stylesheet" href="/css/app.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" />
</head>
<body class="bg-slate-200 text-slate-900 font-mono">
    <%- include('../partials/admin/header') %>
    
    <div class="max-w-2xl mx-auto p-4 md:p-8 mt-12">
        
        <div class="bg-white border-2 border-slate-900 brutalist-shadow-dark p-8">
            
            <h1 class="text-2xl font-extrabold uppercase mb-6 border-b-2 border-slate-900 pb-2">Edit Category</h1>
            
            <form action="/admin/categories/edit/<%= category.id %>" method="POST" class="space-y-6">
                
                <div>
                    <label class="block font-bold uppercase mb-2">Name</label>
                    <input type="text" name="name" value="<%= category.name %>" required 
                        class="w-full px-4 py-3 border-2 border-slate-900 font-bold focus:ring-4 focus:ring-cyan-400 focus:outline-none">
                </div>
                
                <div>
                    <label class="block font-bold uppercase mb-2">Font Awesome Icon Class</label>
                    <input type="text" name="icon" value="<%= category.icon %>" 
                        class="w-full px-4 py-3 border-2 border-slate-900 font-bold focus:ring-4 focus:ring-cyan-400 focus:outline-none">
                    
                    <p class="text-xs mt-2 text-slate-500">Preview: <i class="<%= category.icon %> ml-1"></i></p>
                </div>
                
                <div class="flex gap-4">
                    <button type="submit" class="flex-1 bg-fuchsia-500 text-white py-3 border-2 border-slate-900 font-bold uppercase hover:bg-fuchsia-600">
                        Save Changes
                    </button>
                    
                    <a href="/admin/categories" class="flex-1 text-center py-3 border-2 border-slate-900 font-bold uppercase hover:bg-slate-100">
                        Cancel
                    </a>
                </div>
            </form>
        </div>
    </div>
</body>
</html>
