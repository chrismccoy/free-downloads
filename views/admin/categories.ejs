<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Categories // Admin</title>
    <link rel="stylesheet" href="/css/app.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" />
</head>
<body class="bg-slate-200 text-slate-900 font-mono">
    <%- include('../partials/admin/header') %>
    
    <div class="max-w-7xl mx-auto p-4 md:p-8">
        
        <div class="bg-white border-2 border-slate-900 brutalist-shadow-dark p-6 mb-12">
            <h2 class="text-xl font-extrabold uppercase mb-4 border-b-2 border-slate-900 pb-2">
                <i class="fa-solid fa-tag text-fuchsia-500 mr-2"></i> New Category
            </h2>
            
            <form action="/admin/categories/add" method="POST" class="flex flex-col md:flex-row gap-4">
                
                <div class="flex-1">
                    <input type="text" name="name" placeholder="CATEGORY NAME..." required 
                        class="w-full px-4 py-3 border-2 border-slate-900 bg-slate-50 focus:outline-none focus:ring-4 focus:ring-fuchsia-500 font-bold">
                </div>
                
                <div class="flex-1">
                    <input type="text" name="icon" placeholder="ICON CLASS (e.g. fa-solid fa-code)..." 
                        class="w-full px-4 py-3 border-2 border-slate-900 bg-slate-50 focus:outline-none focus:ring-4 focus:ring-fuchsia-500 font-bold">
                </div>
                
                <button type="submit" class="bg-slate-900 text-white px-8 py-3 border-2 border-slate-900 font-bold uppercase hover:bg-cyan-400 hover:text-slate-900 transition-colors">
                    Add
                </button>
            </form>
        </div>

        <div class="bg-white border-2 border-slate-900 p-6">
            <h2 class="text-2xl font-extrabold uppercase mb-6">Existing Categories</h2>
            
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
                <% categories.forEach(cat => { %>
                    <div class="border-2 border-slate-900 p-4 bg-slate-50 flex justify-between items-center group hover:bg-white transition-colors">
                        
                        <div class="flex items-center gap-3">
                            <i class="<%= cat.icon %> text-2xl text-cyan-500"></i>
                            <div>
                                <h3 class="font-bold uppercase"><%= cat.name %></h3>
                                <p class="text-xs text-slate-500">/<%= cat.slug %></p>
                            </div>
                        </div>

                        <div class="flex gap-2">
                            <a href="/admin/categories/edit/<%= cat.id %>" class="text-slate-900 hover:text-fuchsia-600 font-bold border-2 border-transparent hover:border-slate-900 px-2">
                                <i class="fa-solid fa-pen"></i>
                            </a>
                            
                            <form action="/admin/categories/delete/<%= cat.id %>" method="POST" class="inline">
                                <button type="submit" class="text-slate-400 hover:text-red-600 font-bold border-2 border-transparent hover:border-red-600 px-2">
                                    <i class="fa-solid fa-trash"></i>
                                </button>
                            </form>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>
