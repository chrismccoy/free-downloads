<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Login // Admin</title>
    <link rel="stylesheet" href="/css/app.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" />
</head>
<body class="bg-slate-200 text-slate-900 font-mono flex items-center justify-center min-h-screen p-4">
    
    <div class="w-full max-w-md bg-white border-2 border-slate-900 brutalist-shadow-dark p-8">
        
        <div class="mb-8 text-center">
            <i class="fa-solid fa-fingerprint text-6xl text-cyan-400 mb-4"></i>
            <h1 class="text-4xl font-extrabold uppercase">System Access</h1>
            <p class="text-slate-500 font-bold mt-2">Authorized Personnel Only</p>
        </div>
        
        <% if(locals.error) { %>
            <div class="bg-red-100 border-2 border-red-500 text-red-600 p-4 mb-6 font-bold text-center uppercase text-sm">
                <i class="fa-solid fa-ban mr-1"></i> Access Denied
            </div>
        <% } %>

        <form action="/admin/login" method="POST" class="space-y-6">
            
            <div>
                <label class="block font-bold uppercase mb-2 text-xs">Identity</label>
                <input type="text" name="username" class="w-full px-4 py-3 border-2 border-slate-900 bg-slate-50 focus:bg-white focus:outline-none focus:ring-4 focus:ring-fuchsia-500 font-bold" required>
            </div>
            
            <div>
                <label class="block font-bold uppercase mb-2 text-xs">Key</label>
                <input type="password" name="password" class="w-full px-4 py-3 border-2 border-slate-900 bg-slate-50 focus:bg-white focus:outline-none focus:ring-4 focus:ring-fuchsia-500 font-bold" required>
            </div>
            
            <button class="w-full bg-slate-900 text-white py-4 font-bold uppercase border-2 border-slate-900 hover:bg-cyan-400 hover:text-slate-900 transition-colors shadow-lg">
                Authenticate
            </button>
        </form>

        <div class="mt-8 text-center border-t-2 border-slate-200 pt-4">
            <a href="/" class="text-xs font-bold uppercase text-slate-400 hover:text-slate-900">&larr; Return to Public Site</a>
        </div>
    </div>

</body>
</html>
